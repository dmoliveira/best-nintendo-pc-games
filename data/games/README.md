# Game records

Add one reviewed JSON game record per slug here after the platform/genre taxonomy and source policy are ready. The catalog validator accepts an empty directory during foundation work and will reject unknown platform, genre, source, signal, link, or asset references once records are added.
